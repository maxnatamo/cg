# Input presets folder.
# Each file should contain formatted variables, e.g. "${*foreground}" for
# replacement with "*foreground" variable.
presets_folder      = "./presets/"

# Folder for output config files. The output files will have the same name as the input files.
output_folder       = "~/.cache/cg/"

# Default .Xresources config file, if no file is specified.
default             = "~/.Xresources"

# Log level
# Values: 0-2
#    0 (none)
#    1 (warning)
#    2 (log)
# Default value: 1
log_level           = 1
